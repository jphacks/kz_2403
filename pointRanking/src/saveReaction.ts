export interface ReactionData {
  userId: string;
  userName: string;
  messageId: string;
  messageText: string;
  messageUserId: string;
  channelId: string;
  reactionUserId: string;
  reactionId: string;
  emojiId: string;
  emojiName: string;
  resultMonth: string;
  points: number;
}
